# format for opening a file
# handle = open(filename, mode)
# open - opens file, the filename
# read - opens the file to read only
# write - opens the file so you can write it
# close - closes the file when you are done
# append - add information to the end of a file

file_handle = open('testfile.txt', 'r')
print(file_handle)